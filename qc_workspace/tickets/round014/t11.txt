TICKET 11 of 11  |  round 14
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
[P0] Text Legibility And Contrast

The previously flagged Shared/Press/news_collision.html no longer fails: its visible publication line and copyright are both styled #666 on white, rather than the earlier #777/#888 values. A separate current defect remains across eleven PDFs: their visible running headers, footers, Bates labels, or page numbers use #808080 on a white page. That combination is only about 3.95:1 contrast, below the normal-text threshold, so the page chrome is not sufficiently legible under this dimension; darken the gray running-chrome style (for example, to #666 or darker) in each affected document/template.
