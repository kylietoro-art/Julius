TICKET 8 of 10  |  round 4
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
[P0] Format-Native Feature Fidelity

Format-native fidelity materially fails in three separately fixable ways. RunSchedule_Owl512.xlsx uses a Status column without color/status encoding, TCR.pdf leaves the collision-report fields and visual-diagram slot empty, and the clearly marked preliminary incident-report draft has no comments or tracked changes. These defects make status meaning nonvisual, preserve a blank reserved visual area in an essential case artifact, and strip the working-state markup that a document presenting as DRAFT must retain.

Finding #1: Unencoded Status column in RunSchedule_Owl512.xlsx (Minor)
Finding #2: TCR.pdf is an uncompleted collision-report template (Major)
Finding #3: Draft incident report lacks working-state markup (Major)
