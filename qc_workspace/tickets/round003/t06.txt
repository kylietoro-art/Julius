TICKET 6 of 6  |  round 3
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
[P0] Format-Native Feature Fidelity

`ACTD/Safety/IR_2025-1018_draft.docx` visibly presents itself as a draft: its extracted opening text reads "On-Scene Incident Report — Preliminary DRAFT — ON-SCENE — SUBJECT TO SUPERVISOR REVIEW." Package inspection found `comments []` and zero `ins`, `del`, `moveFrom`, and `moveTo` revision elements in that same file. This fails sub-check (d) because a reader of the stated working draft has no visible comments or tracked changes showing its review/working state; adding meaningful comments or tracked revision marks is the smallest repair.
