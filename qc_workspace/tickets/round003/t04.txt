TICKET 4 of 6  |  round 3
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
[P1] CSV Formatted Correctly

The CSV corpus fails the required one-header/one-record structure in two files. In `ACTD/Litigation/production_tracker.csv`, row 8 (PT-07) has unquoted commas in its notes field, producing 13 fields despite the header defining 10. In `ACTD/Claims/medical_billing_ledger.csv`, the final row is a `TOTAL` aggregate rather than an itemized billing record, which is prohibited bottom-summary content and creates a mixed record type.

Minor issues:
