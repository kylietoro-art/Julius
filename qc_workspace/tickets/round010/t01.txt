TICKET 1 of 8  |  round 10
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
[P1] No Broken Document References

The preserved native DriveCam clip is an unresolved supporting material. `ACTD/Safety/IR_2025-1018_final.docx` lists a "DriveCam still-frame log ...; native clip retained by vendor under litigation hold, not yet retrieved," and `ACTD/Video/onboard_video_still_log.pdf` identifies the file as `4177_20251018_0130-0210_INC.mp4`, stating that the underlying MP4 is preserved and available from the custodians. The mounted inventory contains only the PDF still-frame log and no video/media file, while the selected world spec defines A53 as "MP4 (clip export) + still-frame log"; thus an explicit, material document reference does not resolve. Add the preserved MP4 or a faithful accessible clip export to `ACTD/Video`.
Add to rerun
