TICKET 12 of 14  |  round 1
==============================================================================

THE FINDING, VERBATIM. Read this immediately before you fix it, and quote it
back in full. Then list every separate thing it asserts, one line each. A
finding that says three things needs three lines, and a fix that answers two
of them is not done.

------------------------------------------------------------------------------
* `ACTD/Safety/IR_2025-1018_draft.docx` visibly identifies itself as "DRAFT — ON-SCENE — SUBJECT TO SUPERVISOR REVIEW." Its package contains no comments and no `<w:ins>` or `<w:del>` tracked-change elements, with `trackRevisions` disabled. A clearly draft Word document therefore has no visible comment or redline machinery, directly failing Format-Native Feature Fidelity sub-check (d).
